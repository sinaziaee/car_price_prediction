# name: CI Pipeline

# # Trigger the workflow on push to any branch and pull requests to main
# on:
#   push:
#     branches: [ "*" ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   test:
#     runs-on: ubuntu-latest
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4
      
#     - name: Set up Python 3.12
#       uses: actions/setup-python@v4
#       with:
#         python-version: '3.12'
        
#     - name: Install uv
#       run: |
#         curl -LsSf https://astral.sh/uv/install.sh | sh
#         echo "$HOME/.local/bin" >> $GITHUB_PATH
        
#     - name: Install dependencies
#       run: |
#         uv sync
        
#     - name: Run linting
#       run: |
#         uv run python -m flake8 src/ main.py bentoml_service.py --max-line-length=100 --ignore=E203,W503 || true
        
#     - name: Run data preprocessing test
#       run: |
#         # Test if preprocessing works with sample data
#         uv run python main.py preprocess --dataset datasets/ca-dealers-used.csv --output-dir data/processed_test
        
#     - name: Run model training test
#       run: |
#         # Test if training works
#         uv run python main.py train-eval --data-dir data/processed_test --model-dir src/models_test
        
#     - name: Run inference test
#       run: |
#         # Test if inference works
#         uv run python main.py infer --input-file input.json --model-dir src/models_test --data-dir data/processed_test
        
#     - name: Test BentoML service
#       run: |
#         # Test if BentoML service can be imported and initialized
#         uv run python -c "
#         import sys
#         sys.path.append('.')
#         try:
#             from bentoml_service import CarPricePrediction
#             print('✅ BentoML service imports successfully')
#         except Exception as e:
#             print(f'❌ BentoML service import failed: {e}')
#             exit(1)
#         "

#   build-docker:
#     runs-on: ubuntu-latest
#     needs: test
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4
      
#     - name: Build Docker image
#       run: |
#         docker build -t car-price-prediction:${{ github.sha }} .
        
#     - name: Test Docker image
#       run: |
#         # Test if the container starts successfully
#         timeout 30s docker run --rm car-price-prediction:${{ github.sha }} bentoml --help || true
#         echo "✅ Docker image builds and runs successfully"